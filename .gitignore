# Local env and secrets (never commit)
.env
.env.*
!.env.example

# Kubernetes secrets often named like this
*secret*.yaml
*-secrets.yaml
secrets.yaml

# Cluster credentials
kubeconfig
*kubeconfig*
.kube/

# Keys and certs
*.pem
*.key
id_rsa
id_ed25519
